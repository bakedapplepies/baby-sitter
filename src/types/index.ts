import { ImageSourcePropType } from "react-native";

export type Babysitter = {
  id: number;
  name: string;
  pfp: ImageSourcePropType;
}

export type User = {
  id: number;
  name: string;
  pfp: ImageSourcePropType;
}